function main() {
    // Initialize the clock
    initializeClock();

    // Initialize tooltips for desktop icons
    initializeTooltips();
}

// Run the main function once the DOM is fully loaded
document.addEventListener('DOMContentLoaded', main);
